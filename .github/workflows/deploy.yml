name: deploy

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements.txt
      - run: pytest -q

  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          port: ${{ secrets.SSH_PORT }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script_stop: true
          script: |
            cd /root/rem-deploer
            git fetch --prune origin
            git reset --hard origin/main
            docker compose up -d --build
            # entrypoint.sh/конфиг vault примонтированы как отдельные файлы, а
            # git reset --hard заменяет их новым inode. up -d не пересоздаёт vault,
            # поэтому контейнер держит старый inode и крутит прежний скрипт.
            # Форсим пересоздание, чтобы правки vault применялись сами. Тома
            # (vaultdata и пр.) при этом не трогаются — секреты сохраняются.
            docker compose up -d --force-recreate vault
            docker image prune -f
